<Window x:Class="积微.Views.TimelineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:积微.Controls"
        Title="时间线"
        Icon="pack://application:,,,/Resources/icon/番茄.png"
        Width="550"
        Height="660"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Background}">
    <Window.Resources>
        <Style x:Key="ThinScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="6"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MaxWidth" Value="6"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Width" Value="10"/>
                    <Setter Property="MaxWidth" Value="10"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InteractiveButton" TargetType="Button">
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                               To="1.05" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                               To="1.05" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                               To="1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                               To="1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CustomComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource HoverColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Border x:Name="OuterBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource BorderColor}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <ToggleButton x:Name="ToggleButton"
                                              Grid.Column="0"
                                              Grid.ColumnSpan="2"
                                              Focusable="False"
                                              IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press"
                                              Background="Transparent">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="Transparent" Padding="0"/>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                
                                <ContentPresenter x:Name="ContentSite"
                                                  Grid.Column="0"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                  Margin="10,0,5,0"
                                                  VerticalAlignment="Center"
                                                  HorizontalAlignment="Left"
                                                  IsHitTestVisible="False"/>
                                
                                <Path x:Name="Arrow"
                                      Grid.Column="1"
                                      Data="M0,3 L3,6 L6,3"
                                      Fill="{DynamicResource TextSecondary}"
                                      Margin="0,0,8,0"
                                      HorizontalAlignment="Right"
                                      VerticalAlignment="Center"
                                      Width="6"
                                      Height="6"
                                      IsHitTestVisible="False"/>
                                
                                <Popup x:Name="Popup"
                                       Placement="Bottom"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       AllowsTransparency="True"
                                       Focusable="False"
                                       PopupAnimation="Slide">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource CardBackground}"
                                            BorderBrush="{DynamicResource BorderColor}"
                                            BorderThickness="1"
                                            CornerRadius="10">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="8" Opacity="0.1" ShadowDepth="2" Direction="270"/>
                                        </Border.Effect>
                                        <ScrollViewer Margin="0">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="OuterBorder" Property="Background" Value="{DynamicResource HoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="CustomComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource HoverColor}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentColor}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InteractiveInputBorder" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="12" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="20" ShadowDepth="4"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#0A84FF" Opacity="0.1" BlurRadius="20" ShadowDepth="4"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TimelineEntryCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="12" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.Opacity)" 
                                               To="0.08" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)" 
                                               To="20" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.ShadowDepth)" 
                                               To="4" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                                               To="-2" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.Opacity)" 
                                               To="0.04" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)" 
                                               To="12" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.ShadowDepth)" 
                                               To="2" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                                               To="0" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CustomDatePicker" TargetType="DatePicker">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="12" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Grid>
                            <Border x:Name="PART_Root" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    Background="{TemplateBinding Background}" 
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <DatePickerTextBox x:Name="PART_TextBox" 
                                                      Grid.Column="0" 
                                                      HorizontalContentAlignment="Stretch" 
                                                      VerticalContentAlignment="Center" 
                                                      FontSize="{TemplateBinding FontSize}" 
                                                      Foreground="{TemplateBinding Foreground}" 
                                                      BorderThickness="0" 
                                                      Background="Transparent"/>
                                    <Button x:Name="PART_Button" 
                                            Grid.Column="1" 
                                            Width="32" 
                                            Height="32" 
                                            Background="Transparent" 
                                            BorderThickness="0" 
                                            Focusable="False">
                                        <Path Data="M19,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3M19,15H17V13H15V15H13V13H11V15H9V13H7V15H5V7H19V15Z" 
                                              Fill="{DynamicResource TextTertiary}" 
                                              Width="16" 
                                              Height="16" 
                                              Stretch="Uniform"/>
                                    </Button>
                                </Grid>
                            </Border>
                            <Popup x:Name="PART_Popup" 
                                   Placement="Bottom" 
                                   PlacementTarget="{Binding ElementName=PART_Root}" 
                                   StaysOpen="False">
                                <Border BorderBrush="{DynamicResource BorderColor}" 
                                        BorderThickness="1" 
                                        Background="{DynamicResource CardBackground}" 
                                        CornerRadius="8">
                                    <Calendar x:Name="PART_Calendar" 
                                              Margin="2" 
                                              DisplayMode="Month" 
                                              SelectionMode="SingleDate"/>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="PART_Root" Property="BorderBrush" Value="#9CA3AF"/>
                                <Setter TargetName="PART_Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="20" ShadowDepth="4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="PART_Root" Property="BorderBrush" Value="#3B82F6"/>
                                <Setter TargetName="PART_Root" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="PART_Root" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#3B82F6" Opacity="0.1" BlurRadius="20" ShadowDepth="4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="20" Background="{DynamicResource Background}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ScrollViewer.Resources>
                <Style x:Key="ThinScrollBar" TargetType="ScrollBar">
                    <Setter Property="Width" Value="6"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="MaxWidth" Value="6"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Width" Value="10"/>
                            <Setter Property="MaxWidth" Value="10"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Resources>
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollViewer">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ScrollContentPresenter Grid.Column="0" Content="{TemplateBinding Content}" ClipToBounds="False"/>
                                    <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Column="1"
                                               Orientation="Vertical"
                                               Value="{TemplateBinding VerticalOffset}"
                                               Maximum="{TemplateBinding ScrollableHeight}"
                                               ViewportSize="{TemplateBinding ViewportHeight}"
                                               Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                               Style="{StaticResource ThinScrollBar}"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ScrollViewer.Style>
            <Grid Margin="0,0,16,0" HorizontalAlignment="Stretch" MaxWidth="500">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" HorizontalAlignment="Stretch" Margin="0,20,0,28">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" x:Name="TitleTextBlock" Text="时间线" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" MaxWidth="300"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                        <Button x:Name="SortButton" Width="40" Height="32" 
                                Click="SortButton_Click"
                                VerticalAlignment="Center" Style="{StaticResource InteractiveButton}" ToolTip="倒序">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="8" Background="{DynamicResource HoverColor}" Padding="8">
                                        <Path x:Name="SortIcon" Data="M6,12L12,6L18,12" Stroke="{DynamicResource TextSecondary}" StrokeThickness="2" StrokeLineJoin="Round" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="ShowAddButton" Width="40" Height="32" 
                                Margin="10,0,0,0" Click="ShowAddButton_Click"
                                VerticalAlignment="Center" Style="{StaticResource InteractiveButton}" ToolTip="添加">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="8" Background="{DynamicResource HoverColor}" Padding="8">
                                        <Path x:Name="AddIcon" Data="M12,5V19M5,12H19" Stroke="{DynamicResource TextSecondary}" StrokeThickness="2" StrokeLineJoin="Round" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
                
                <StackPanel Grid.Row="1" x:Name="InputPanel" Margin="12,0,0,28" Visibility="Collapsed">
                    <Border Grid.Column="0" Style="{StaticResource InteractiveInputBorder}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBox x:Name="TimelineInputTextBox"
                                     Grid.Row="0"
                                     Width="Auto" MinHeight="48"
                                     BorderBrush="Transparent" BorderThickness="0"
                                     Padding="16" FontSize="14"
                                     VerticalContentAlignment="Top"
                                     Background="Transparent"
                                     Foreground="{DynamicResource TextPrimary}"
                                     CaretBrush="{DynamicResource TextPrimary}"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     HorizontalScrollBarVisibility="Disabled"
                                     TextAlignment="Left"/>
                            
                            <ItemsControl x:Name="ImagePreviewPanel"
                                          Grid.Row="1"
                                          Margin="16,8,16,16"
                                          Visibility="Collapsed">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,8,8" CornerRadius="8" Background="{DynamicResource SecondaryBackground}" Cursor="Hand">
                                            <Grid>
                                                <Image Source="{Binding}" MaxHeight="100" MaxWidth="150"
                                                       Stretch="Uniform" StretchDirection="DownOnly"
                                                       MouseLeftButtonDown="PreviewImage_MouseLeftButtonDown"
                                                       Tag="{Binding}"/>
                                                <Button Content="×"
                                                        HorizontalAlignment="Right" VerticalAlignment="Top"
                                                        Margin="0,4,4,0"
                                                        Width="20" Height="20"
                                                        Background="#EF4444" Foreground="White"
                                                        FontSize="12" FontWeight="Bold"
                                                        Cursor="Hand"
                                                        Click="RemoveImage_Click"
                                                        Tag="{Binding}">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border CornerRadius="10" Background="{TemplateBinding Background}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <Grid Grid.Row="2" Margin="16,8,16,16" VerticalAlignment="Bottom">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <Button x:Name="AddImageButton" Grid.Column="0" Width="36" Height="36"
                                        Background="{DynamicResource HoverColor}" Foreground="{DynamicResource TextSecondary}"
                                        Click="AddImageButton_Click"
                                        Style="{StaticResource InteractiveButton}" ToolTip="添加图片">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border CornerRadius="8" Background="{TemplateBinding Background}" Padding="8">
                                                <Path Data="M21,19V5C21,3.89 20.1,3 19,3H5A2,2 0 0,0 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19M8.5,13.5L11,16.51L14.5,12L19,18H5L8.5,13.5"
                                                      Fill="{TemplateBinding Foreground}"
                                                      Width="16" Height="16" Stretch="Uniform"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>

                                <ComboBox x:Name="TimelineTypeComboBox" Grid.Column="1" Width="70" Height="36" 
                          Margin="8,0,0,0" 
                          Style="{StaticResource CustomComboBox}"
                          SelectedIndex="0">
                    <ComboBoxItem Style="{StaticResource CustomComboBoxItem}" Content="想法" />
                    <ComboBoxItem Style="{StaticResource CustomComboBoxItem}" Content="问题" />
                </ComboBox>
                                
                                <StackPanel Grid.Column="3" Orientation="Horizontal">
                                    <Button x:Name="CancelTimelineButton" Content="取消" Width="60" Height="36"
                                            Background="{DynamicResource HoverColor}" Foreground="{DynamicResource TextSecondary}"
                                            Margin="0,0,8,0"
                                            Click="CancelTimelineButton_Click">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border CornerRadius="8" Background="{TemplateBinding Background}" Padding="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                    <Button x:Name="SaveTimelineButton" Content="保存" Width="60" Height="36"
                                            Background="#10B981" Foreground="White"
                                            Click="SaveTimelineButton_Click"
                                            Style="{StaticResource InteractiveButton}">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border CornerRadius="8" Background="{TemplateBinding Background}" Padding="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </StackPanel>
                
                <Grid Grid.Row="2" x:Name="FilterPanel" Margin="12,0,0,20" Panel.ZIndex="1" ClipToBounds="False">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                            <Button x:Name="FilterAllButton" Content="全部" Width="60" Height="32" 
                                    Background="#3B82F6" Foreground="White" 
                                    Click="FilterAllButton_Click"
                                    VerticalAlignment="Center" Style="{StaticResource InteractiveButton}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="8" Background="{TemplateBinding Background}" Padding="10,5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button x:Name="FilterThoughtButton" Content="想法" Width="60" Height="32" 
                                    Background="{DynamicResource SecondaryBackground}" Foreground="{DynamicResource SecondaryText}" 
                                    Margin="10,0,0,0" Click="FilterThoughtButton_Click"
                                    VerticalAlignment="Center" Style="{StaticResource InteractiveButton}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="8" Background="{TemplateBinding Background}" Padding="10,5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button x:Name="FilterQuestionButton" Content="问题" Width="60" Height="32" 
                                    Background="{DynamicResource SecondaryBackground}" Foreground="{DynamicResource SecondaryText}" 
                                    Margin="10,0,0,0" Click="FilterQuestionButton_Click"
                                    VerticalAlignment="Center" Style="{StaticResource InteractiveButton}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="8" Background="{TemplateBinding Background}" Padding="10,5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                            <Button x:Name="FilterOperationButton" Content="操作" Width="60" Height="32" 
                                    Background="{DynamicResource SecondaryBackground}" Foreground="{DynamicResource SecondaryText}" 
                                    Margin="10,0,0,0" Click="FilterOperationButton_Click"
                                    VerticalAlignment="Center" Style="{StaticResource InteractiveButton}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="8" Background="{TemplateBinding Background}" Padding="10,5">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </StackPanel>
                        <Border Grid.Column="2" Style="{StaticResource InteractiveInputBorder}" Margin="0,0,0,0" VerticalAlignment="Center">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Path Grid.Column="0" Data="M15.5,14H14.71L14.43,13.73C15.41,12.59 16,11.11 16,9.5C16,5.91 13.09,3 9.5,3C5.91,3 3,5.91 3,9.5C3,13.09 5.91,16 9.5,16C11.11,16 12.59,15.41 13.73,14.43L14,14.71V15.5L19,20.49L20.49,19L15.5,14Z" 
                                      Fill="{DynamicResource TextTertiary}" Width="16" Height="16" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBox x:Name="SearchTextBox" Grid.Column="1" Width="Auto" Height="32" 
                                         BorderBrush="Transparent" BorderThickness="0" 
                                         Padding="0,0,8,0" FontSize="14" 
                                         VerticalContentAlignment="Center" 
                                         Background="Transparent" 
                                         Foreground="{DynamicResource TextPrimary}"
                                         CaretBrush="{DynamicResource TextPrimary}"
                                         TextWrapping="NoWrap" 
                                         TextAlignment="Left" 
                                         TextChanged="SearchTextBox_TextChanged" 
                                         ToolTip="搜索时间线内容"/>
                            </Grid>
                        </Border>
                    </Grid>
                    <Grid Grid.Row="1" HorizontalAlignment="Stretch" Margin="0,10,0,0" Panel.ZIndex="1" ClipToBounds="False">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="时间段：" FontSize="14" Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <controls:CalendarDatePicker x:Name="StartDatePicker" Grid.Column="1" Width="120"
                                                    DateChanged="StartDatePicker_DateChanged" 
                                                    ToolTip="开始日期"/>
                        <TextBlock Grid.Column="2" Text="至" FontSize="14" Foreground="{DynamicResource SecondaryText}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <controls:CalendarDatePicker x:Name="EndDatePicker" Grid.Column="3" Width="120"
                                                    DateChanged="EndDatePicker_DateChanged" 
                                                    ToolTip="结束日期"/>
                        <Button x:Name="ClearDateFilterButton" Grid.Column="4" Content="重置" Width="45" Height="32" 
                                Background="{DynamicResource SecondaryBackground}" Foreground="{DynamicResource SecondaryText}" 
                                Margin="10,0,0,0" Click="ClearDateFilterButton_Click"
                                VerticalAlignment="Center" Style="{StaticResource InteractiveButton}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="8" Background="{TemplateBinding Background}" Padding="10,5">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>
                </Grid>

                <StackPanel Grid.Row="3" x:Name="TimelinePanel" />
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>