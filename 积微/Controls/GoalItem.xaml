<UserControl x:Class="积微.Controls.GoalItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="100"
             d:DesignWidth="680">
    <UserControl.Resources>
        <Style x:Key="InteractiveGoalCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource CardBackground}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="0"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.04" BlurRadius="12" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.Opacity)" 
                                               To="0.08" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)" 
                                               To="20" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.ShadowDepth)" 
                                               To="4" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                                               To="-2" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.Opacity)" 
                                               To="0.04" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)" 
                                               To="12" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.ShadowDepth)" 
                                               To="2" Duration="0:0:0.2"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" 
                                               To="0" Duration="0:0:0.2"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InteractiveBadge" TargetType="Border">
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#EF4444" Opacity="0" BlurRadius="0" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               To="1.25" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               To="1.25" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.Opacity)"
                                               To="0.35" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)"
                                               To="8" Duration="0:0:0.12"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               To="1" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               To="1" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.Opacity)"
                                               To="0" Duration="0:0:0.12"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.Effect).(DropShadowEffect.BlurRadius)"
                                               To="0" Duration="0:0:0.12"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InteractiveButton" TargetType="Button">
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               To="1.08" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               To="1.08" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                               To="1" Duration="0:0:0.1"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                               To="1" Duration="0:0:0.1"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="0,6,0,6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Border x:Name="GoalBorder" Style="{StaticResource InteractiveGoalCard}">
                <Border.ContextMenu>
                    <ContextMenu x:Name="GoalContextMenu" StaysOpen="True">
                        <MenuItem Header="添加" Click="AddSubgoalMenuItem_Click"/>
                        <MenuItem Header="详情" Click="DetailMenuItem_Click"/>
                        <MenuItem Header="时间线" Click="TimelineMenuItem_Click"/>
                        <Separator Style="{StaticResource LabeledSeparator}"/>
                        <MenuItem Header="置为顶层目标" Click="SetAsTopLevelMenuItem_Click"/>
                        <MenuItem Header="修改父目标" x:Name="ParentGoalMenuItem" SubmenuOpened="ParentGoalMenuItem_SubmenuOpened">
                            <MenuItem Header="加载中..." IsEnabled="False"/>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="删除" Click="DeleteMenuItem_Click" Foreground="#EF4444"/>
                    </ContextMenu>
                </Border.ContextMenu>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                        
                        <Button x:Name="ExpandButton" Grid.Column="0" Width="28" Height="28" Margin="0,0,10,0" Click="ExpandButton_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="Transparent">
                                        <TextBlock x:Name="ExpandIcon" Text="▲" FontSize="14" Foreground="{DynamicResource TextTertiary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        
                        <TextBlock x:Name="GoalTitle" Grid.Column="1" FontSize="17" FontWeight="Medium" Foreground="{DynamicResource TextPrimary}" VerticalAlignment="Center"/>
                        
                        <TextBlock x:Name="DurationTextBlock" Grid.Column="2" FontSize="13" Foreground="{DynamicResource TextTertiary}" Margin="12,0,12,0" VerticalAlignment="Center"/>
                        
                        <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- 完成一次按钮 - 仅对重复目标显示 -->
                        <Button x:Name="CompleteOnceButton" Width="68" Height="30" Margin="0,0,8,0" Style="{StaticResource InteractiveButton}" Visibility="Collapsed">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid Width="68" Height="30">
                                        <Border x:Name="BtnBorder" CornerRadius="7" Background="#3B82F6">
                                            <TextBlock Text="完成一次" FontSize="11" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border x:Name="ProgressOverlay" CornerRadius="7" Background="#10FFFFFF" Visibility="Collapsed">
                                            <Grid Width="68" Height="30">
                                                <Rectangle x:Name="ProgressTrack" Fill="#22000000" RadiusX="7" RadiusY="7"/>
                                                <Border x:Name="ProgressFill" CornerRadius="7" HorizontalAlignment="Left" Width="0">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#E0FFFFFF" Offset="0"/>
                                                            <GradientStop Color="#FFFFFFFF" Offset="0.5"/>
                                                            <GradientStop Color="#E0FFFFFF" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="CompleteButton" Width="54" Height="30" Margin="0,0,8,0" Style="{StaticResource InteractiveButton}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid Width="54" Height="30">
                                        <Border x:Name="BtnBorder" CornerRadius="7" Background="#10B981">
                                            <TextBlock Text="完成" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border x:Name="ProgressOverlay" CornerRadius="7" Background="#10FFFFFF" Visibility="Collapsed">
                                            <Grid Width="54" Height="30">
                                                <Rectangle x:Name="ProgressTrack" Fill="#22000000" RadiusX="7" RadiusY="7"/>
                                                <Border x:Name="ProgressFill" CornerRadius="7" HorizontalAlignment="Left" Width="0">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#E0FFFFFF" Offset="0"/>
                                                            <GradientStop Color="#FFFFFFFF" Offset="0.5"/>
                                                            <GradientStop Color="#E0FFFFFF" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="FailButton" Width="54" Height="30" Margin="0,0,8,0" Style="{StaticResource InteractiveButton}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid Width="54" Height="30">
                                        <Border x:Name="BtnBorder" CornerRadius="7" Background="#EF4444">
                                            <TextBlock Text="失败" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border x:Name="ProgressOverlay" CornerRadius="7" Background="#10FFFFFF" Visibility="Collapsed">
                                            <Grid Width="54" Height="30">
                                                <Rectangle x:Name="ProgressTrack" Fill="#22000000" RadiusX="7" RadiusY="7"/>
                                                <Border x:Name="ProgressFill" CornerRadius="7" HorizontalAlignment="Left" Width="0">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#E0FFFFFF" Offset="0"/>
                                                            <GradientStop Color="#FFFFFFFF" Offset="0.5"/>
                                                            <GradientStop Color="#E0FFFFFF" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="StartButton" Width="54" Height="30" Margin="0,0,8,0" Visibility="Collapsed" Style="{StaticResource InteractiveButton}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid Width="54" Height="30">
                                        <Border x:Name="BtnBorder" CornerRadius="7" Background="#10B981">
                                            <TextBlock Text="开始" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border x:Name="ProgressOverlay" CornerRadius="7" Background="#10FFFFFF" Visibility="Collapsed">
                                            <Grid Width="54" Height="30">
                                                <Rectangle x:Name="ProgressTrack" Fill="#22000000" RadiusX="7" RadiusY="7"/>
                                                <Border x:Name="ProgressFill" CornerRadius="7" HorizontalAlignment="Left" Width="0">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#E0FFFFFF" Offset="0"/>
                                                            <GradientStop Color="#FFFFFFFF" Offset="0.5"/>
                                                            <GradientStop Color="#E0FFFFFF" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="PendingButton" Width="54" Height="30" Margin="0,0,8,0" Style="{StaticResource InteractiveButton}">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Grid Width="54" Height="30">
                                        <Border x:Name="BtnBorder" CornerRadius="7" Background="#F59E0B">
                                            <TextBlock Text="搁置" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <Border x:Name="ProgressOverlay" CornerRadius="7" Background="#10FFFFFF" Visibility="Collapsed">
                                            <Grid Width="54" Height="30">
                                                <Rectangle x:Name="ProgressTrack" Fill="#22000000" RadiusX="7" RadiusY="7"/>
                                                <Border x:Name="ProgressFill" CornerRadius="7" HorizontalAlignment="Left" Width="0">
                                                    <Border.Background>
                                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                                            <GradientStop Color="#E0FFFFFF" Offset="0"/>
                                                            <GradientStop Color="#FFFFFFFF" Offset="0.5"/>
                                                            <GradientStop Color="#E0FFFFFF" Offset="1"/>
                                                        </LinearGradientBrush>
                                                    </Border.Background>
                                                </Border>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
                </Border>
            
            <!-- 右上角角标 - 显示重复目标完成次数 -->
            <Border x:Name="CompletionBadge" HorizontalAlignment="Right" VerticalAlignment="Top"
                    Margin="0,-8,-8,0" Width="20" Height="20" CornerRadius="10"
                    Background="#EF4444" Visibility="Collapsed"
                    Style="{StaticResource InteractiveBadge}">
                <TextBlock x:Name="CompletionCountText" Text="0" FontSize="11" FontWeight="Bold"
                           Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
        </Grid>
        
        <StackPanel x:Name="ChildrenPanel" Grid.Row="1" Margin="36,10,0,0" Visibility="Collapsed"/>
    </Grid>
</UserControl>
